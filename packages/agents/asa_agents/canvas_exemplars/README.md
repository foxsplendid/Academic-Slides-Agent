# Canvas exemplars (vendored, MIT)

Full-page SVG composition exemplars injected as few-shot style references into the premium-tier
canvas authoring prompt. Vendored from CRui5in/paper-ppt-agent at commit
`6f679fc2ad410597e2e582ceab2a21c72f6bf773` (MIT — see packages/vendor/svg2pptx/README.md for the
full provenance note; this is the same snapshot). Unmodified copies; the prompt instructs the model
to keep the layout craft but swap colors to our palette and use only evidence data.
