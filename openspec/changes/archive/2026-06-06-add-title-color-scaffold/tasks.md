## 1. Title color + scaffold profile

- [x] 1.1 `StyleProfile.title_rgb` (optional) + `add_rich_text(color=…)`; titles use it
- [x] 1.2 `PPTAGENT_ACADEMIC` profile from the academic_defense design tokens (marked temporary scaffold)
- [x] 1.3 Export profile; tests (title color applied; default unchanged); suite green
